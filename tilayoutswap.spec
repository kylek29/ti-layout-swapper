# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['tilayoutswap.py'],
             pathex=['C:\\_mega\\Documents\\_Code\\Python\\BBT_TradeIdeas_ScannerSwap\\_script_version'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='tilayoutswap',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=True , icon='tilayoutswap.ico')
